{
  "name": "azure-notification-hubs-nodejs-example",
  "version": "0.0.1",
  "main": "./dist/index.mjs",
  "type": "module",
  "license": "0BSD",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "exec": "npm run build && node .",
    "fmt": "prettier --write .",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "azure-sb": "^0.11.2",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/azure-sb": "^0.0.39",
    "@types/prettier": "^2.7.0",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.2"
  }
}
